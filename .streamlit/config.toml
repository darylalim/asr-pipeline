[server]
maxUploadSize = 500

# No [theme] section: the app uses Streamlit's built-in light and dark themes.
# Leaving theming unconfigured keeps the light/dark switcher in the app's
# settings menu and lets the app track upstream restyles automatically.
